#!/bin/sh
#!/bin/sh
docker run --rm --name=mcp_jenkins_client --network=host -it -p 5001:5000 -e JENKINS_URL=${JENKINS_URL} -e JENKINS_USER=${JENKINS_USER} -e JENKINS_API_TOKEN=${JENKINS_API_TOKEN} jenkins_mcp_server /usr/local/bin/python "/app/src/jenkins_mcp_server/chat_interface.py" "$@"
